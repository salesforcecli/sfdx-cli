name: automerge
on:
  workflow_dispatch:
  schedule:
    - cron: '18 2,5,8,11 * * *'

jobs:
  automerge:
    uses: salesforcecli/github-workflows/.github/workflows/automerge.yml@re/rename-mac-pkg
    with:
      mergeMethod: squash
    secrets: inherit
