name: automerge-dependabot
on:
  workflow_dispatch:
  schedule:
    - cron: '18 2,5,8,11 * * *'

jobs:
  automerge:
    uses: salesforcecli/github-workflows/.github/workflows/automerge.yml@main
    with:
      mergeMethod: squash
    secrets: inherit
