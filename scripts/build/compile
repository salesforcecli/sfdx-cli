#!/usr/bin/env bash

source ./scripts/_init

set -ex

tsc -p "$ROOT_DIR/tsconfig.json"
cp "$ROOT_DIR/src/builtins/package.json" "$ROOT_DIR/dist/builtins"
cd "$ROOT_DIR/dist/builtins"
"$ROOT_DIR/node_modules/.bin/oclif-dev" manifest
